<div>
    <div class="d-flex justify-content-center">
    <h2 class="page-title">Home</h2>
    </div>
    <ul class="list-group">
        <li class="list-group-item">{{board.name}} created by {{user.username}}</li>
        <li class="list-group-item">{{board.name}} created by {{user.username}}</li>
        <li class="list-group-item">{{board.name}} created by {{user.username}}</li>
    </ul>
    
</div>