<div>
    <div class="d-flex justify-content-center">
    <h2 class="page-title">Home</h2>
    </div>
    <ul class="list-group">
        {{#each boards as |board|}}
        <a href='/board/{{board.slug}}'><li class="list-group-item">{{board.name}} <p class="bold">created by {{board.user.username}}</p></li></a>
        {{/each}}
    </ul>
    
</div>